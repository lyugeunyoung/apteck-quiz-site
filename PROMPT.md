# apteck-quiz-site — 퀴즈 페이지 전면 재설계 지시서 (Claude Code용)

> **사용법**: 저장소 루트에 `PROMPT.md`(또는 `docs/spec.md`)로 커밋해 두고, Claude Code 실행 시
> `이 저장소의 PROMPT.md를 정독하고 Phase 0부터 순서대로 진행해줘` 라고만 입력하세요.
> 회사/재택 어느 PC에서 시작하든 같은 지시가 재현됩니다.

---

## 0. 역할 정의

당신은 다음 5개 역할을 동시에 수행하는 시니어 실무자입니다. 모든 산출물은 이 5개 관점의 교차 검증을 통과해야 합니다.

| 역할 | 책임 |
|---|---|
| **시니어 풀스택 개발자** | 정적 사이트 아키텍처, 빌드 파이프라인, GitHub Actions, 장애 복구 |
| **UX 설계자** | 방문자 동선(정답까지 3초) + **관리자 동선(하루 3분 이내 운영)** |
| **한국형 SEO 전문가** | 구글 / **네이버(최우선 난제)** / AI 검색(AEO·GEO) 동시 최적화 |
| **애드센스 수익화 전문가** | 정책 위반 0건 전제하의 뷰어빌리티·RPM 극대화 |
| **QA 엔지니어** | 자동 검증 스크립트 + 시나리오 시뮬레이션, 통과할 때까지 자가 수정 |

**말투/보고 규칙**: 한국어, 간결하게. 각 Phase 종료 시 `무엇을 왜 바꿨는지 / 검증 결과 / 남은 리스크` 3줄 요약 후 다음 Phase 진행.

---

## 1. 프로젝트 사실관계 (반드시 코드로 확인 후 시작)

- **저장소**: `https://github.com/lyugeunyoung/apteck-quiz-site` (Public)
- **스택**: 빌드툴·서버·DB 없는 순수 정적 HTML/CSS/JS + GitHub Pages
- **데이터 원본(Source of Truth)**: `data/quizzes.json`
- **렌더러**: `assets/js/template.js` (Node·브라우저 겸용) → `scripts/build.js`가 `index.html`, `pages/*.html`, `sitemap.xml`, `feed.xml` 재생성
- **관리자**: `admin.html` + `assets/js/admin.js` (GitHub API 직접 커밋, PAT 사용)
- **자동화**: `.github/workflows/sync-sheet.yml` + `scripts/sync-sheet.js` (구글시트 CSV → JSON → 재빌드 → 커밋)
- **콘텐츠**: 토스·카카오뱅크·카카오페이·신한SOL·KB Pay·하나원큐·NH올원뱅크·케이뱅크·비트버니 등 **21개 앱테크 퀴즈**
- **참고 자산**: 기존 티스토리 운영본 — `https://bookshelf-journey.tistory.com/category/앱테크 포인트 모으기`
  (문장 톤, 목차 구조, 지난 정답 아카이브 UX, 회차 처리 방식을 **분석만** 하고 그대로 복사하지 말 것 — 3.6 중복 콘텐츠 항목 참조)

**Phase 0 (착수 전 필수)**: 위 파일들을 전부 읽고 `docs/AS-IS.md`에 현행 구조·데이터 스키마·알려진 문제 10가지를 정리해 커밋할 것. 추측으로 코드를 고치지 말 것.

---

## 2. 절대 원칙 (Non-negotiables) — 위반 시 즉시 되돌릴 것

1. **정답 본문은 100% 정적 HTML이어야 한다.**
   구글시트 fetch를 **런타임(브라우저)에서 하지 않는다.** 시트 → JSON → HTML 변환은 전부 **빌드 타임(GitHub Actions / `scripts/build.js`)**에서 끝낸다.
   → 이유: 네이버 Yeti 크롤러는 JS 렌더링 의존 콘텐츠를 사실상 수집하지 못한다. 네이버 노출이 이 프로젝트의 수익 전제인 이상 이건 타협 불가다.
   (클라이언트 JS는 정답 펼치기·공유·즐겨찾기 같은 **인터랙션 전용**. JS를 꺼도 문제·정답·해설 전문이 HTML에 보여야 한다.)
2. **관리자 편의성 > 코드 우아함.** 관리자가 하루에 하는 조작은 **"시트에 입력 → 끝"** 이거나 **"버튼 1회 클릭"** 이어야 한다. 2단계를 넘기면 설계를 다시 하라.
3. **모든 작업은 GitHub에 즉시 반영.** 로컬에만 남는 산출물 금지. Phase 단위로 커밋·푸시, 작업 시작 전 `git pull --rebase`.
4. **정책 위반 리스크가 있는 구현은 수익보다 우선해서 차단한다.** (애드센스 계정 정지 = 프로젝트 사망)
5. **깨지는 코드를 "아마 될 것"으로 넘기지 않는다.** Phase 6 검증을 통과할 때까지 자가 수정 반복.

---

## 3. 작업 지시

### 3.1 Phase 1 — 퀴즈 상세 페이지 UI 전면 재검토·재설계

현재 `pages/*.html`의 정보구조를 아래 순서로 재구성한다. **순서 자체가 SEO·수익 설계**이므로 임의 변경 금지.

| # | 블록 | 요구사항 |
|---|---|---|
| 1 | 상단 상태 바 | `오늘 갱신됨 / 갱신 대기` 배지 + **갱신 시각(KST, `<time datetime>`)** + 앱 브랜드 컬러 |
| 2 | H1 | `{앱이름} 정답 (M월 D일)` — 실검 패턴 그대로. 연도·수식어 금지 |
| 3 | **정답 요약 카드** | 스크롤 없이 3초 안에 정답이 보이는 **결론 우선** 카드. 회차 앱은 회차별 정답 나란히 |
| 4 | 광고 A | 정답 카드 **아래**(위 아님 — 2.4 참조), display, `min-height` 예약 |
| 5 | 오늘의 문제 | 문제 전문 + 보기 + 문제 이미지(있을 때) |
| 6 | 정답 & 해설 | `<details>` 네이티브 요소로 펼치기(JS 없이 동작), 해설은 **직접 작성한 2~4문장** |
| 7 | 광고 B | in-article |
| 8 | 참여 방법 | 앱 열기 딥링크/원링크 CTA + 참여 시간·리워드·소요시간 표 |
| 9 | 지난 정답 아카이브 | 최근 14~30회차, **실제 HTML 텍스트**(아코디언이라도 DOM에 존재해야 함) |
| 10 | FAQ | 페이지별로 문구가 다른 4~6문항 (`FAQPage` 스키마와 1:1 일치) |
| 11 | 다른 앱테크 퀴즈 | 내부링크 그리드(전 앱 상호링크) — 회유율·크롤 깊이 개선 |
| 12 | 광고 C | 하단 multiplex/autorelaxed |
| 13 | 푸터 | 운영자·문의·개인정보처리방침·면책 고지 |

**디자인 요구**
- 모바일 우선(360~430px 기준으로 먼저 그리고 데스크톱 확장). 실사용의 90%는 모바일이다.
- 저채도 배색 + 앱별 시그니처 컬러 1개만 포인트로. 원색 남발 금지.
- 터치 타깃 최소 44×44px, 본문 16px 이상, 명암비 4.5:1 이상.
- **CLS 방지**: 모든 `<img>`에 `width`/`height`, 모든 광고 컨테이너에 `min-height` 고정.
- 다크모드 대응(`prefers-color-scheme`).
- 아이콘·장식은 CSS/인라인 SVG로. 외부 폰트·아이콘 CDN 추가 금지(LCP 방어).

### 3.2 Phase 2 — 데이터 파이프라인 & 이미지 자산화 ➕(추가 제안)

**요구하신 "클릭 한 번 → 시트 실시간 반영"은 유지하되, 이미지는 저장소 자산으로 승격시킨다.**

- 구글 드라이브는 이미지 CDN이 아니다. 트래픽 급증 시 "다운로드 한도 초과"로 이미지가 죽고, 검색엔진 재크롤링 때도 같은 문제가 난다. 파일 이동·권한 변경 한 번이면 과거 페이지 전부가 깨진다.
- **해결(관리자 편의성은 그대로)**: 관리자는 지금처럼 드라이브 링크만 붙여넣는다. → GitHub Actions가 그 링크를 **다운로드 → WebP 변환(최대 폭 1200px) → `assets/img/quiz/{app-id}/{YYYY-MM-DD}.webp`로 커밋** → 페이지는 저장소 내부 경로를 참조. 드라이브는 "입력 통로"로만 쓰고 "서빙 경로"에서 빼라.
- 다운로드 실패 시 원본 드라이브 URL로 폴백하고, 워크플로 요약에 경고를 남길 것.

**동기화 워크플로 보강**
- `cron`은 UTC 기준이므로 **KST 오픈 시각(예: 08:00·10:00·12:00·20:00) 직후에 촘촘히, 야간에는 성기게** 스케줄링. 날짜 계산은 전부 `Asia/Seoul` 고정.
- `concurrency: group` 설정으로 동시 실행 충돌 차단, 봇 커밋에 `[skip ci]` 처리로 무한 루프 방지.
- **콘텐츠 변화가 없으면 커밋하지 않는다**(현재 날짜만 바뀌어 매일 빈 커밋이 생기는 문제 수정).
- 시트 스키마 검증기 추가: `app_id` 미매칭, 날짜 형식 오류, 정답 공백, 회차값 오타를 **커밋 전에** 잡아 워크플로를 실패시키고 이유를 명시.

### 3.3 Phase 3 — 관리자 페이지(admin.html) 재설계

관리자 편의성이 이 프로젝트의 **1순위 요구사항**이다.

**필수 기능**
1. **원클릭 동기화**: `🔄 오늘 시트 전체 불러오기` 버튼 하나로 21개 앱의 오늘 행을 한 번에 읽고 → 변경분 diff 표시 → `전체 게시` 1클릭 커밋. 앱을 하나씩 선택해 반복 입력하는 현재 동선을 없앨 것.
2. **오늘의 대시보드**: 상단에 `등록 완료 n / 대기 m` + 미등록 앱만 필터. 무엇이 남았는지 3초 안에 파악되게.
3. **회차 앱 지원 통일**: 수동 미리보기 경로도 `round_time`을 지원(현재 미지원 — 수정).
4. **저장 전 실시간 미리보기**: 실제 페이지와 동일 렌더러(`template.js`)로 렌더. 미리보기와 결과가 다르면 안 됨.
5. **입력 검증**: 정답 공백, 해설 30자 미만, 이미지 링크 형식 오류, HTML 태그가 문자로 노출되는 케이스를 저장 버튼에서 차단.
6. **원클릭 롤백** ➕: 직전 커밋 기준으로 `data/quizzes.json`을 되돌리는 버튼. 잘못 게시했을 때 관리자가 git을 몰라도 복구 가능해야 한다.
7. **오류 메시지의 인간화**: `422 Unprocessable Entity` 대신 `토큰 권한이 부족합니다 → Contents: Read and write로 재발급하세요`.

**보안** ➕
- PAT를 `localStorage`에 평문 보관하는 현재 방식은 XSS 한 방에 저장소 쓰기 권한이 털린다. 기본값을 **세션 저장(sessionStorage)** 으로 바꾸고, "이 기기에 저장"은 명시적 옵트인 + 만료일 안내로 처리.
- `admin.html`에 `<meta name="robots" content="noindex,nofollow">` + `X-Robots-Tag` 대체 수단 명시, `robots.txt` 차단 유지.
- 토큰은 fine-grained·해당 저장소 한정·만료 90일 권장을 UI에 안내.
- (선택 개선안으로 제시만 할 것) Cloudflare Pages Functions로 토큰을 서버측에 두는 안 — 채택 여부는 사용자 승인 후.

### 3.4 Phase 4 — 검색 최적화: 구글 · AI 검색

**구글**
- 페이지별 고유 `title`/`description`/`canonical`. 제목 패턴은 실제 상위 노출 기사와 동일하게 `{앱이름} 정답 (M월 D일)`.
- 구조화 데이터(JSON-LD): `Article`(`datePublished`/`dateModified` — **최신성 신호가 이 사이트의 핵심 무기**), `BreadcrumbList`, `FAQPage`, `ItemList`(앱 목록), `WebSite`+`Organization`(사이트 단위 1회), `SpeakableSpecification`(정답 요약 카드 지정).
  - `Quiz` 스키마는 교육 콘텐츠용이므로 **오용 금지**.
  - JSON-LD의 모든 값은 화면에 실제로 보이는 텍스트와 일치해야 한다(불일치 = 구조화 데이터 스팸).
- **URL 전략 결정** ➕: 허브 URL(`/pages/toss-luck.html`)은 고정·매일 갱신 + `canonical` self. 날짜별 아카이브 페이지는 **고유 해설이 충분할 때만** 생성하고, 얇으면 만들지 말 것(양산형 페이지는 스팸 판정 위험). 이 결정을 `docs/DECISIONS.md`에 근거와 함께 기록.
- `sitemap.xml`의 `lastmod`를 실제 갱신 시각으로 정확히. `feed.xml`은 갱신순 정렬.
- **IndexNow** 연동 ➕ (Bing/Yandex 즉시 수집. 네이버·구글은 미지원이므로 기대치를 문서에 명시).

**AI 검색(AEO·GEO)** — 2026년 구글 AI 개요·AI 모드, 네이버 AI 브리핑·AI 탭이 인용할 수 있는 형태로 만든다.
- 루트에 **`llms.txt`** 생성: 사이트 정체성 1문단 + 21개 앱 허브 URL 목록 + 갱신 주기 명시. (순위 상승 파일이 아니라 "이 사이트가 무엇인지"를 AI에 정의해주는 보조 파일이라는 점을 문서에 정확히 기록할 것.)
- **답변 가능 형태로 문장 설계**: 각 섹션 첫 문장이 그 자체로 완결된 답이 되게 쓴다.
  - 나쁨: `오늘의 정답을 아래에서 확인해보세요.`
  - 좋음: `2026년 9월 10일 토스 행운퀴즈 정답은 '○○○'입니다. 오전 6시에 출제되며 참여 시 최대 100원이 적립됩니다.`
- 표·정의형 문장·FAQ·숫자를 많이 쓴다. LLM은 구조화된 짧은 사실 문장을 인용한다.
- **엔티티 명시**: 앱 정식명칭·운영사·공식 퀴즈명(예: 카카오뱅크 "AI 생각하는 퀴즈")을 본문 첫 등장 시 정확히 표기.
- AI 크롤러 접근 정책을 `robots.txt`에 명시적으로 정리(허용 대상 명확화 — Yeti 포함).
- **정답 스니펫 노출 트레이드오프**: 정답을 `description`/JSON-LD에 넣으면 검색·AI 노출은 유리하지만 클릭 없이 소비되어 광고 수익이 줄 수 있다. → `data/quizzes.json`의 `site.exposeAnswerInMeta: true|false` **플래그로 전환 가능하게 구현**하고, 두 모드의 예상 효과를 `docs/DECISIONS.md`에 정리. 초기값은 `true`(우선 트래픽 확보), 4주 뒤 GSC 데이터로 재판단.

### 3.5 Phase 5 — 네이버 대책 (★ 별도 최우선 과제)

기존 티스토리 페이지가 **구글에는 뜨는데 네이버에는 안 뜨는 문제**가 반복됐다. 독립 도메인에서는 원인이 다르므로 아래를 전부 처리한다.

1. **커스텀 도메인 확보 (강력 권장)** ➕
   `*.github.io` 하위 경로는 네이버에서 독립 사이트로 신뢰받기 불리하고, 사이트 소유확인·수집 정책에서도 제약이 있다. 저렴한 도메인(예: `.kr`, `.com`)을 연결하고 GitHub Pages에 `CNAME` + HTTPS 강제. **이 항목은 사용자 결정이 필요하므로 Phase 5 착수 시 먼저 질문할 것.**
2. **서치어드바이저 필수 절차 자동화 가능한 부분 구현**
   - 소유확인 메타태그 값을 `site.naverSiteVerification`으로 주입(이미 존재 — 값 입력 안내 UI를 admin에 노출).
   - **사이트맵 + RSS 둘 다 제출**. 네이버는 RSS 기반 신규 수집이 빠르므로, 매일 바뀌는 이 사이트에는 RSS가 사이트맵보다 중요하다. `feed.xml`에 전문(문제·정답·해설)을 담고 `pubDate`를 정확히.
   - 수집 요청(웹마스터도구 > 요청)은 API가 없어 수동이다. **`docs/NAVER-CHECKLIST.md`에 매일/주간 수동 작업 체크리스트를 만들어 관리자가 3분 안에 끝내게 할 것.**
3. **오픈그래프 완전성**: 네이버는 OG 태그 의존도가 높다. `og:title`/`og:description`/`og:image`(1200×630, 절대경로)/`og:url`/`og:type`/`og:site_name` 전부 채우고, 이미지가 없어도 기본 OG 이미지가 항상 존재하게.
4. **JS 의존 제거**(2.1 원칙 재확인) — Yeti가 읽을 본문이 HTML에 그대로 있어야 한다. 빌드 후 `curl`로 받은 HTML에 정답 문자열이 존재하는지 자동 검사하는 테스트를 추가할 것.
5. **`robots.txt`에 `User-agent: Yeti` 명시 허용** + `Sitemap:` 절대경로.
6. **네이버가 싫어하는 패턴 제거**: 동일 문구 대량 복제, 기계적 자동생성 티가 나는 문서, 키워드 반복 나열, 광고 대비 본문 부족. → 앱마다 **고정 서술 콘텐츠(참여방법·리워드 구조·회차 시간표·앱 특징·주의사항)를 최소 800자 이상 서로 다르게** 확보해 매일 바뀌는 정답 한 줄에만 의존하지 않게 한다.
7. **티스토리와의 자기잠식(cannibalization) 방지** ➕ — **중요**
   같은 정답 콘텐츠를 티스토리와 신규 사이트에 중복 게시하면 네이버에서 둘 다 밀린다. 역할을 분리하라:
   - **티스토리**: 짧은 요약 + 정답 한 줄 + `https://{신규도메인}` 원문 링크(네이버 친화 채널, 유입 깔때기)
   - **신규 사이트**: 전체 정보 허브(문제·해설·아카이브·FAQ·참여방법)
   - 두 채널의 제목·리드 문장은 서로 다르게 생성한다. `docs/DECISIONS.md`에 이 분리 원칙을 명문화.
8. **성과 계측**: 네이버 유입은 서치어드바이저 + GA4의 referrer로만 확인 가능. 4주 관찰 계획을 체크리스트에 포함.

### 3.6 Phase 6 — 애드센스 수익화 & 정책 방어

- **게시자 ID / 슬롯** (기존 자산 — 실제 값은 사용자 확인 후 반영):
  `ca-pub-8223351968323727`, 슬롯 `8415308925`(상단 display) / `8443755743`(중단 display) / `8420459614`(in-article) / `7602201961`(하단 autorelaxed)
  → `data/quizzes.json`의 `site.adsense`에 구조화해 넣고 `template.js`의 `adSlot()`에서 주입. 하드코딩 금지.
- **정책 방어(위반 시 계정 정지)**
  - 정답 확인 버튼 **바로 위/아래에 광고를 붙여 오클릭을 유도하지 말 것.** 정답 카드와 광고 사이 최소 24px 여백 + 시각적 구분선.
  - 광고를 콘텐츠처럼 보이게 하는 스타일링 금지. 라벨은 `광고` 또는 `스폰서 링크`만 허용.
  - 페이지당 광고 3~4개 상한, 첫 화면(above the fold)이 광고로 뒤덮이지 않게.
  - 자동광고를 켤 경우 앵커·전면광고가 정답 열람을 방해하지 않는지 실기기 확인.
  - **저품질·자동생성 판정 방어**: 정답 한 줄짜리 얇은 페이지 21개는 위험 신호다. 3.5-6의 고정 서술 콘텐츠 + 직접 작성 해설로 방어.
  - **저작권**: 앱 화면 스크린샷 무단 게시는 리스크다. 문제는 **직접 요약·재작성**하고, 이미지는 자체 제작 그래픽을 우선한다. 각 페이지에 출처·면책 문구를 넣을 것.
- **필수 정적 페이지**: `privacy.html`(보유), `terms.html`, `about.html`(운영자·연락처 — E-E-A-T), `contact` 수단. `ads.txt` 실제 pub-id 반영.
- **CLS = 수익**: 광고 로드로 레이아웃이 밀리면 오클릭·이탈·랭킹 모두 손해. 슬롯 `min-height` 예약 필수.

### 3.7 Phase 7 — 운영 자동화 & 모니터링 ➕

- **미갱신 감시**: 매일 KST 특정 시각에 "오늘 데이터가 없는 앱"을 검출해 **GitHub Issue 자동 생성**(또는 워크플로 실패). 관리자가 빠뜨린 걸 사이트가 알려줘야 한다.
- **링크 헬스체크**: 주 1회 딥링크·이미지 URL 200 응답 확인.
- **`docs/DECISIONS.md`**: 왜 그렇게 정했는지(ADR). 다른 PC에서 이어받을 때 이 파일이 컨텍스트다.
- **`README.md` 갱신**: 변경된 동선 반영. 회사↔재택 이어받기 절차(`git pull --rebase` → 작업 → `git push`) 유지·보강.
- **`CHANGELOG.md`**: 버전·날짜·변경요약.

---

## 4. 검증 프로토콜 (Phase 6 — 통과할 때까지 자가 수정)

### 4.1 자동 검증 (`scripts/verify.js` 신규 작성, `npm test`로 실행)

빌드 후 생성물에 대해 아래를 프로그램으로 확인하고, **실패 항목이 0이 될 때까지 수정→재실행을 반복**한다.

1. 모든 `pages/*.html`에 정답 문자열이 **정적 텍스트로** 존재하는가 (JS 없이)
2. `title`/`description`/`canonical`/OG 6종/네이버·구글 인증 메타가 모두 존재하고 페이지마다 고유한가
3. JSON-LD가 유효 JSON이며 필수 필드 누락이 없고, 표기값이 본문 텍스트와 일치하는가
4. `h1`이 정확히 1개이고 heading 위계(h1→h2→h3)에 건너뜀이 없는가
5. 모든 `img`에 `alt`·`width`·`height`가 있는가
6. `sitemap.xml`/`feed.xml`의 URL이 실재하고 `lastmod`/`pubDate`가 유효한가
7. 내부 링크 깨짐 0건, `baseUrl` 하드코딩 잔재 0건
8. 광고 슬롯 개수가 상한(4) 이내이고 각 슬롯에 `min-height`가 지정됐는가
9. 페이지별 고정 서술 콘텐츠가 800자 이상인가 (얇은 콘텐츠 방어)
10. `admin.html`이 `robots.txt`와 메타 양쪽에서 차단되는가

### 4.2 시나리오 시뮬레이션 (각 시나리오 **최소 5회** 반복, 결과를 `docs/QA-REPORT.md`에 표로 기록)

| # | 시나리오 | 합격 기준 |
|---|---|---|
| S1 | 시트에 오늘 행 입력 → 동기화 실행 | 해당 앱 페이지·홈·sitemap·feed가 모두 갱신, 무관한 앱은 무변경 |
| S2 | 관리자 원클릭 전체 게시 | 21개 앱 diff 정상 표시, 커밋 1회로 종료, 30~60초 내 반영 |
| S3 | 회차 앱(카카오뱅크 AI 생각하는 퀴즈) 3회차 순차 입력 | 미입력 회차는 "아직 등록되지 않았습니다" 표기, 기존 회차 보존 |
| S4 | 잘못된 시트 데이터(날짜 오류·app_id 오타·정답 공백) | 커밋되지 않고 이유가 명확히 출력 |
| S5 | 드라이브 이미지 링크 입력 | WebP 자산화 커밋 성공, 실패 시 폴백 + 경고 |
| S6 | JS 비활성 브라우저로 페이지 열기 | 문제·정답·해설 전문이 그대로 보임 |
| S7 | 어제 정답 → 오늘 정답 갱신 | 어제 정답이 아카이브로 이동, 유실 0 |
| S8 | 모바일 360px / 데스크톱 1440px | 가로 스크롤 0, 터치 타깃 44px 이상, CLS 시각적 밀림 없음 |
| S9 | 원클릭 롤백 | 직전 상태로 정확히 복구 |
| S10 | 두 PC 동시 작업(회사 push 후 재택 pull) | 충돌 없이 이어받기, Actions 봇 커밋과 충돌 없음 |

**리치 결과 검증**: 생성된 JSON-LD를 구글 Rich Results Test 규격에 맞춰 자체 파서로 점검하고, 사용자가 브라우저에서 확인할 URL 목록을 `docs/QA-REPORT.md` 말미에 제시할 것.

---

## 5. Git 워크플로

```bash
git pull --rebase origin main       # 작업 시작 전 항상
# ... Phase 작업 ...
node scripts/build.js && npm test   # 검증 통과 확인
git add -A
git commit -m "feat(ui): 퀴즈 상세 페이지 정보구조 재설계 (Phase 1)"
git push origin main
```

- 커밋 메시지는 Conventional Commits(`feat|fix|refactor|docs|chore(scope): 요약`).
- **Phase 완료마다 커밋·푸시.** 여러 Phase를 몰아 한 커밋으로 만들지 말 것.
- 리스크 큰 변경(도메인 전환, 광고 배치 변경)은 별도 브랜치 → 확인 후 머지.

---

## 6. 완료 정의 (Definition of Done)

- [ ] `docs/AS-IS.md`, `docs/DECISIONS.md`, `docs/QA-REPORT.md`, `docs/NAVER-CHECKLIST.md` 생성·커밋
- [ ] `scripts/verify.js` 10개 항목 전부 통과 (실패 0)
- [ ] S1~S10 시나리오 각 5회 반복 전부 합격
- [ ] 21개 앱 페이지 전부 재빌드, 고정 서술 콘텐츠 800자 이상 확보
- [ ] `llms.txt`, `terms.html`, `about.html` 신규 생성
- [ ] `README.md`·`CHANGELOG.md` 갱신
- [ ] 전부 `main`에 push 완료
- [ ] 사용자가 직접 해야 할 일(도메인 결정, 서치어드바이저·서치콘솔 등록, 애드센스 값 입력)을 **번호 매긴 체크리스트**로 최종 보고

---

## 7. 진행 중 반드시 사용자에게 물어볼 것 (임의 결정 금지)

1. 커스텀 도메인을 붙일 것인가 (네이버 노출에 직결 — Phase 5 착수 전 질문)
2. 정답을 메타/JSON-LD에 노출할 것인가 (트래픽 ↔ 광고 수익 트레이드오프)
3. 티스토리와 신규 사이트의 역할 분리안 승인 여부
4. 애드센스 자동광고 사용 여부
5. 문제 이미지를 앱 스크린샷으로 쓸 것인가, 자체 제작 그래픽으로 대체할 것인가 (저작권)

**Phase 0부터 시작하고, 각 Phase 종료 시 3줄 요약 보고 후 다음으로 넘어갈 것.**
